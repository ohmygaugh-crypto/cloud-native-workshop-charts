apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "baseline-vue-app.fullname" . }}-test-connection"
  labels:
    {{- include "baseline-vue-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "baseline-vue-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
