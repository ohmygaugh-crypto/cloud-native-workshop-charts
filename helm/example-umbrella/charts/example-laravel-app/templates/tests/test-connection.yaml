apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "example-laravel-app.fullname" . }}-test-connection"
  labels:
    {{- include "example-laravel-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "example-laravel-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
