apiVersion: v1
kind: Service
metadata:
  name: {{ include "example-umbrella.fullname" . }}
  labels:
    {{- include "example-umbrella.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "example-umbrella.selectorLabels" . | nindent 4 }}
