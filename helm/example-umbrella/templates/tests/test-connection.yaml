apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "example-umbrella.fullname" . }}-test-connection"
  labels:
    {{- include "example-umbrella.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "example-umbrella.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
